<UserControl x:Class="UniversityScheduler.Views.StatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800" >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="1" Columns="2" Rows="2" VerticalAlignment="Top">
            
            <Border Background="{StaticResource BackgroundBrush1}" Margin="0,0,20,20" CornerRadius="10" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="{StaticResource SidebarBrush}" CornerRadius="10,10,0,0" Padding="15">
                        <TextBlock Text="Total Instructors" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="16"/>
                    </Border>
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="InstructorCount" Text="0" FontSize="48" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Active Faculty" Foreground="Gray" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="{StaticResource BackgroundBrush1}" Margin="0,0,0,20" CornerRadius="10" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="{StaticResource SidebarBrush}" CornerRadius="10,10,0,0" Padding="15">
                        <TextBlock Text="Total Courses" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="16"/>
                    </Border>
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="CourseCount" Text="0" FontSize="48" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Subjects Registered" Foreground="Gray" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="{StaticResource BackgroundBrush1}" Margin="0,0,20,0" CornerRadius="10" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="{StaticResource SidebarBrush}" CornerRadius="10,10,0,0" Padding="15">
                        <TextBlock Text="Student Sections" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="16"/>
                    </Border>
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="SectionCount" Text="0" FontSize="48" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Classes to Schedule" Foreground="Gray" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="{StaticResource BackgroundBrush1}" Margin="0,0,0,0" CornerRadius="10" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="{StaticResource SidebarBrush}" CornerRadius="10,10,0,0" Padding="15">
                        <TextBlock Text="Total Rooms" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="16"/>
                    </Border>
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="RoomCount" Text="0" FontSize="48" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Available Facilities" Foreground="Gray" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

        </UniformGrid>
    </Grid>
</UserControl>