<UserControl x:Class="UniversityScheduler.Views.ScheduleTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             >

    <Border BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" Margin="5" Background="{StaticResource BackgroundBrush1}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SidebarBrush}" Padding="10">
                <TextBlock x:Name="TableTitle" 
                           Text="Schedule Table" 
                           FontWeight="SemiBold" 
                           FontSize="16" 
                           Foreground="{StaticResource BackgroundBrush1}" 
                           HorizontalAlignment="Center"/>
            </Border>

            <Grid Grid.Row="1" Background="{StaticResource BackgroundBrush1}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="95"/> <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>   <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="0,0,1,1">
                    <TextBlock Text="TIME" Foreground="{StaticResource BackgroundBrush1}" FontWeight="SemiBold" FontSize="14" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <ScrollViewer x:Name="HeaderScroll" Grid.Row="0" Grid.Column="1" 
                              HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                    <Grid x:Name="HeaderGrid"/>
                </ScrollViewer>

                <ScrollViewer x:Name="TimeScroll" Grid.Row="1" Grid.Column="0" 
                              HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
                    <Grid x:Name="TimeGrid"/>
                </ScrollViewer>

                <ScrollViewer x:Name="MainScroll" Grid.Row="1" Grid.Column="1" 
                              HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                              ScrollChanged="MainScroll_ScrollChanged">
                    <Grid x:Name="ContentGrid"/>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</UserControl>