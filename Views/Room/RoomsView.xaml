<UserControl x:Class="UniversityScheduler.Views.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="600" d:DesignWidth="900"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" CornerRadius="4" Background="White" Height="35">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                    <TextBox x:Name="SearchTxt" Grid.Column="1" BorderThickness="0" VerticalContentAlignment="Center" 
                             Padding="0,0,5,0" Background="Transparent" 
                             TextChanged="SearchTxt_TextChanged"
                             Tag="Search Room Name..."/>
                </Grid>
            </Border>
        </Grid>

        <DataGrid x:Name="RoomsGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False" 
                  BorderThickness="1" 
                  BorderBrush="{StaticResource TextLightBrush}"
                  HorizontalGridLinesBrush="{StaticResource TextLightBrush}"
                  VerticalGridLinesBrush="{StaticResource TextLightBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Floor" Binding="{Binding FloorLevel}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="120" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="140"/>
                <DataGridTextColumn Header="Cap" Binding="{Binding Capacity}" Width="50"/>
                
                <DataGridTextColumn Header="PC" Binding="{Binding ComputerCount}" Width="40" ToolTipService.ToolTip="Computers"/>
                <DataGridTextColumn Header="AC" Binding="{Binding AirConCount}" Width="40" ToolTipService.ToolTip="Air Cons"/>
                
                <DataGridTextColumn Header="Mon" Binding="{Binding MonitorCount}" Width="40" ToolTipService.ToolTip="Monitors"/>
                
                <DataGridTextColumn Header="Proj" Binding="{Binding ProjectorCount}" Width="40" ToolTipService.ToolTip="Projectors"/>
                <DataGridTextColumn Header="WB" Binding="{Binding WhiteboardCount}" Width="40" ToolTipService.ToolTip="Whiteboards"/>
                
                <DataGridTextColumn Header="CF" Binding="{Binding CeilingFanCount}" Width="40" ToolTipService.ToolTip="Ceiling Fans"/>
                <DataGridTextColumn Header="SF" Binding="{Binding StandFanCount}" Width="40" ToolTipService.ToolTip="Stand Fans"/>

                <DataGridTextColumn Header="Chairs" Binding="{Binding ChairCount}" Width="50"/>
                <DataGridTextColumn Header="Tables" Binding="{Binding TableCount}" Width="50"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Reset All" Width="100" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="Reset_Click"/>
            
            <Button Content="Edit Room" Style="{StaticResource SecondaryButton}" Width="140" Height="35" Margin="0,0,10,0" Click="Edit_Click"/>
            <Button Content="Remove Room" Width="140" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
            <Button Content="Add Room" Style="{StaticResource PrimaryButton}" Width="140" Height="35" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</UserControl>