<UserControl x:Class="UniversityScheduler.Views.InstructorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="600" d:DesignWidth="1000"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- NEW: Cascading Dropdown Filters -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="SemToggleButton" Content="Sem 1" Width="65" Height="30" Margin="0,0,15,0" 
                        Style="{StaticResource SecondaryButton}" Click="SemToggleButton_Click" FontWeight="Bold"/>

                <TextBlock Text="Filter:" FontWeight="Bold" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,10,0"/>
                
                <ComboBox x:Name="FilterStatusCombo" Width="100" Height="30" Margin="0,0,10,0" 
                          SelectionChanged="FilterStatusCombo_SelectionChanged" DisplayMemberPath="Display"/>

                <ComboBox x:Name="FilterLoadCombo" Width="110" Height="30" Margin="0,0,10,0" 
                          SelectionChanged="FilterLoadCombo_SelectionChanged" DisplayMemberPath="Display"/>

                <ComboBox x:Name="FilterProgramCombo" Width="100" Height="30" Margin="0,0,10,0" 
                          SelectionChanged="FilterProgramCombo_SelectionChanged" DisplayMemberPath="Display"/>
                          
                <ComboBox x:Name="FilterYearCombo" Width="100" Height="30" Margin="0,0,10,0" 
                          SelectionChanged="FilterYearCombo_SelectionChanged" DisplayMemberPath="Display"/>
                          
                <ComboBox x:Name="FilterSectionCombo" Width="100" Height="30" 
                          SelectionChanged="FilterSectionCombo_SelectionChanged" DisplayMemberPath="Display"/>
            </StackPanel>

            <!-- Search Bar -->
            <Border Grid.Column="1" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" CornerRadius="4" Background="White" Height="35">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                    <TextBox x:Name="SearchTxt" Grid.Column="1" BorderThickness="0" VerticalContentAlignment="Center" 
                             Padding="0,0,5,0" Background="Transparent" 
                             TextChanged="SearchTxt_TextChanged"
                             Tag="Search by name..."/>
                </Grid>
            </Border>
        </Grid>

        <!-- ADDED SelectionMode="Extended" to allow multiple selections via Ctrl-Click or Shift-Click -->
        <ListView x:Name="InstructorsGrid" Grid.Row="1" SelectionMode="Extended" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="4" Padding="8" Width="290" MinHeight="166">
                        
                        <!-- Trigger logic for Glowing Orange Border on Selection -->
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="#BDC3C7"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                                        <Setter Property="BorderBrush" Value="#F57C00"/>
                                        <Setter Property="BorderThickness" Value="2"/>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#F57C00" BlurRadius="6" ShadowDepth="0" Opacity="0.7"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/> 
                                <RowDefinition Height="Auto"/> 
                                <RowDefinition Height="Auto"/> 
                                <RowDefinition Height="*"/>    
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13" Foreground="#2C3E50" TextWrapping="Wrap" Margin="0,0,5,0"/>
                                
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                                    <TextBlock FontWeight="Bold" FontSize="12" HorizontalAlignment="Right">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Setter Property="Text" Value="{Binding UnitLoadDisplay}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOverloaded}" Value="True">
                                                        <Setter Property="Foreground" Value="#C0392B"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding OverloadDisplay}" FontSize="10" Foreground="#C0392B" FontWeight="Bold" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>

                            <WrapPanel Grid.Row="1" Margin="0,0,0,5">
                                <Border BorderBrush="#7F8C8D" BorderThickness="1" CornerRadius="2" Padding="4,1" Margin="0,0,4,4" Background="Transparent">
                                    <TextBlock Text="{Binding Room}" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                                </Border>
                                <Border BorderBrush="#7F8C8D" BorderThickness="1" CornerRadius="2" Padding="4,1" Margin="0,0,4,4" Background="Transparent">
                                    <TextBlock Text="{Binding Program}" FontSize="12" Foreground="#34495E"/>
                                </Border>
                                <Border BorderBrush="#7F8C8D" BorderThickness="1" CornerRadius="2" Padding="4,1" Margin="0,0,4,4" Background="Transparent">
                                    <TextBlock Text="{Binding YearLevels}" FontSize="12" Foreground="#34495E"/>
                                </Border>
                            </WrapPanel>

                            <TextBlock Grid.Row="2" Text="{Binding FormattedTime}" FontSize="12" Foreground="#2C3E50" Margin="0,0,0,5" TextWrapping="Wrap"/>

                            <ScrollViewer Grid.Row="3" MaxHeight="60" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel>
                                    <TextBlock Text="{Binding CoursesStr}" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding SectionsStr}" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" FontWeight="SemiBold"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Clear Settings" Style="{StaticResource DangerButton}" Width="120" Height="35" Margin="0,0,10,0" Click="ClearSettings_Click"/>
            <Button Content="Unassign Schedules" Style="{StaticResource DangerButton}" Width="140" Height="35" Margin="0,0,10,0" Click="UnassignButton_Click"/>
            <Button Content="Reset All" Style="{StaticResource DangerButton}" Width="100" Height="35" Margin="0,0,10,0" Click="Reset_Click"/>
            <Button Content="Edit Instructor" Style="{StaticResource SecondaryButton}" Width="140" Height="35" Margin="0,0,10,0" Click="EditButton_Click"/>
            <Button Content="Remove Instructor" Style="{StaticResource DangerButton}" Width="140" Height="35" Margin="0,0,10,0" Click="DeleteButton_Click"/>
            <Button Content="Add Instructor" Style="{StaticResource PrimaryButton}" Width="140" Height="35" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>