<UserControl x:Class="UniversityScheduler.Views.InstructorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="600" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" CornerRadius="4" Background="White" Height="35">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                    <TextBox x:Name="SearchTxt" Grid.Column="1" BorderThickness="0" VerticalContentAlignment="Center" 
                             Padding="0,0,5,0" Background="Transparent" 
                             TextChanged="SearchTxt_TextChanged"
                             Tag="Search by name..."/>
                </Grid>
            </Border>
        </Grid>

        <DataGrid x:Name="InstructorsGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True" 
                  BorderThickness="1" 
                  BorderBrush="{StaticResource TextLightBrush}"
                  HorizontalGridLinesBrush="{StaticResource TextLightBrush}"
                  VerticalGridLinesBrush="{StaticResource TextLightBrush}"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Initials" Binding="{Binding Initials}" Width="50"/>
                <DataGridTextColumn Header="Room" Binding="{Binding Room}" Width="70"/>
                <DataGridTextColumn Header="Program" Binding="{Binding Program}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Units" Binding="{Binding UnitLoadDisplay}" Width="110" FontWeight="Bold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{Binding LoadColor}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Overload" Binding="{Binding OverloadDisplay}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#C0392B"/> <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Clear Settings" Style="{StaticResource DangerButton}" Width="120" Height="35" Margin="0,0,10,0" Click="ClearSettings_Click"/>
            <Button Content="Unassign Schedules" Style="{StaticResource DangerButton}" Width="140" Height="35" Margin="0,0,10,0" Click="UnassignButton_Click"/>
            <Button Content="Reset All" Style="{StaticResource DangerButton}" Width="100" Height="35" Margin="0,0,10,0" Click="Reset_Click"/>
            <Button Content="Edit Instructor" Style="{StaticResource SecondaryButton}" Width="140" Height="35" Margin="0,0,10,0" Click="EditButton_Click"/>
            <Button Content="Remove Instructor" Style="{StaticResource DangerButton}" Width="140" Height="35" Margin="0,0,10,0" Click="DeleteButton_Click"/>
            <Button Content="Add Instructor" Style="{StaticResource PrimaryButton}" Width="140" Height="35" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>