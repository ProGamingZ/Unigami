<UserControl x:Class="UniversityScheduler.Views.SectionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="600" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" CornerRadius="4" Background="White" Height="35">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                    <TextBox x:Name="SearchTxt" Grid.Column="1" BorderThickness="0" VerticalContentAlignment="Center" 
                             Padding="0,0,5,0" Background="Transparent" 
                             TextChanged="SearchTxt_TextChanged"
                             Tag="Search Program or Class..."/>
                </Grid>
            </Border>
        </Grid>

        <DataGrid x:Name="SectionsGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False" 
                  BorderThickness="1" 
                  BorderBrush="{StaticResource TextLightBrush}"
                  HorizontalGridLinesBrush="{StaticResource TextLightBrush}"
                  VerticalGridLinesBrush="{StaticResource TextLightBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Program" Binding="{Binding Program}" Width="100"/>
                <DataGridTextColumn Header="Year" Binding="{Binding YearLevel}" Width="60"/>
                <DataGridTextColumn Header="Class" Binding="{Binding Name}" Width="80"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullDisplayName}" Width="80" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Units" Binding="{Binding UnitsDisplay}" Width="90" FontWeight="Bold"/>
                <DataGridTextColumn Header="Size" Binding="{Binding StudentCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Reset All" Width="100" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="Reset_Click"/>
            
            <Button Content="Edit Class" Style="{StaticResource SecondaryButton}" Width="140" Height="35" Margin="0,0,10,0" Click="Edit_Click"/>
            <Button Content="Remove Class" Width="140" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
            <Button Content="Add Class" Style="{StaticResource PrimaryButton}" Width="140" Height="35" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</UserControl>