<UserControl x:Class="UniversityScheduler.Views.CurriculumManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" CornerRadius="4" Background="White" Height="35">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                    <TextBox x:Name="SearchTxt" 
                             Grid.Column="1"
                             BorderThickness="0" 
                             VerticalContentAlignment="Center"
                             Background="Transparent"
                             TextChanged="SearchTxt_TextChanged"
                             ToolTip="Search by Program, Course Code, or Year"/>
                </Grid>
            </Border>
        </Grid>

        <ListBox x:Name="CurriculumCardsControl" Grid.Row="1" BorderThickness="0" Background="Transparent" 
                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,10,15"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" 
                                        Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="#3498DB"/> 
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                        <Border Background="White" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                
                                <Grid Margin="10" Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding GroupTitle}" FontSize="16" FontWeight="Bold" 
                                            Foreground="{StaticResource SidebarBrush}" VerticalAlignment="Center"/>

                                    <ToggleButton x:Name="ExpandToggle" Grid.Column="1" IsChecked="{Binding IsExpanded, Mode=TwoWay}" 
                                                Width="30" Height="30" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                        <ToggleButton.Style>
                                            <Style TargetType="ToggleButton">
                                                <Setter Property="Content" Value="▼"/> <Setter Property="FontSize" Value="14"/>
                                                <Setter Property="Foreground" Value="Gray"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsChecked" Value="False">
                                                        <Setter Property="Content" Value="▶"/> </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="Black"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ToggleButton.Style>
                                    </ToggleButton>
                                </Grid>

                                <DataGrid ItemsSource="{Binding Items}" 
                                        Visibility="{Binding IsChecked, ElementName=ExpandToggle, Converter={StaticResource BoolToVis}}"
                                        AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                                        BorderThickness="0" Margin="15,0,15,15"
                                        HorizontalGridLinesBrush="{StaticResource TextLightBrush}"
                                        VerticalGridLinesBrush="Transparent"
                                        RowBackground="White" AlternatingRowBackground="#F9F9F9">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Subject" Binding="{Binding Course.Code}" Width="100" FontWeight="Bold"/>
                                        <DataGridTextColumn Header="Description" Binding="{Binding Course.Name}" Width="350"/>
                                        <DataGridTextColumn Header="Units" Binding="{Binding Course.Units}" Width="60"/>
                                        <DataGridTextColumn Header="Lec" Binding="{Binding Course.LectureHours}" Width="60"/>
                                        <DataGridTextColumn Header="Lab" Binding="{Binding Course.LabHours}" Width="60"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                
                            </StackPanel>
                        </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Reset All" Width="100" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="Reset_Click"/>
            <Button Content="Edit Curriculum" Style="{StaticResource SecondaryButton}" Width="140" Height="35" Margin="0,0,10,0" Click="Edit_Click"/>
            <Button Content="Remove Card" Width="140" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
            <Button Content="Add Curriculum" Style="{StaticResource PrimaryButton}" Width="140" Height="35" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</UserControl>